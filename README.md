# Scan & Score 检录评分系统

一款基于 Flutter 开发的移动端检录评分应用，专为比赛、活动等场景设计，支持条码扫描检录、现场评分和数据导出功能。

## ✨ 功能特性

### 📋 数据管理

- 支持 CSV 文件导入参赛选手数据
- 自动解析选手信息（姓名、参赛编号、组别、头像、领队等）
- 数据本地持久化存储

### 📷 扫码检录

- 使用 Google ML Kit 进行条码/二维码扫描
- 实时相机预览，快速识别作品码
- 一键绑定选手与作品码

### 🏆 现场评分

- 支持扫码快速定位参赛选手
- 输入评分并拍照留存证据
- 自动保存评分照片（命名格式：`作品码_分数.jpg`）

### 📊 数据导出

- 一键导出完整数据（CSV + 评分照片）
- 打包为 ZIP 文件，方便传输
- 支持自定义导出目录

### 📈 数据统计

- 仪表盘实时显示统计数据
- 查看总人数、已检录、已评分等状态
- 照片预览与管理

## 🛠️ 技术栈

- **框架**: Flutter 3.x
- **状态管理**: Provider
- **条码扫描**: google_mlkit_barcode_scanning
- **相机**: camera
- **文件处理**: file_picker, path_provider
- **CSV 解析**: csv
- **压缩导出**: archive
- **权限管理**: permission_handler

## 📱 支持平台

- ✅ Android (推荐)
- ✅ iOS
- ⚠️ Web (部分功能受限)
- ⚠️ macOS / Windows / Linux (桌面端)

## 🚀 快速开始

### 环境要求

- Flutter SDK >= 3.10.1
- Dart SDK >= 3.10.1
- Android Studio / Xcode（用于移动端开发）

### 安装步骤

```bash
# 1. 克隆项目
git clone <repository-url>
cd scan_score

# 2. 获取依赖
flutter pub get

# 3. 运行应用
flutter run

# 4. 构建 Release APK
flutter build apk --release --target-platform android-arm64 --obfuscate --split-debug-info=build/symbols
```

### 安装到设备

```bash
# 查看可用设备
flutter devices

# 安装到指定设备
adb install build/app/outputs/flutter-apk/app-release.apk
```

## 📂 项目结构

```
lib/
├── main.dart              # 应用入口
├── config/                # 配置文件
│   ├── routes.dart        # 路由配置
│   └── theme.dart         # 主题配置
├── models/                # 数据模型
│   └── participant.dart   # 参赛选手模型
├── providers/             # 状态管理
│   ├── auth_provider.dart
│   └── participant_provider.dart
├── screens/               # 页面
│   ├── dashboard/         # 仪表盘
│   ├── checkin/           # 检录页面
│   ├── scoring/           # 评分页面
│   ├── management/        # 管理页面
│   └── export/            # 导出页面
├── services/              # 业务服务
│   ├── storage_service.dart
│   ├── file_service.dart
│   └── csv_service.dart
├── utils/                 # 工具类
└── widgets/               # 通用组件
```

## 📄 CSV 数据格式

导入的 CSV 文件应包含以下列：

| 列名     | 必填 | 说明         |
| -------- | ---- | ------------ |
| 姓名     | ✅   | 参赛选手姓名 |
| 参赛编号 | ✅   | 唯一标识     |
| 组别     | ❌   | 分组信息     |
| 头像名称 | ❌   | 头像文件路径 |
| 领队姓名 | ❌   | 领队信息     |

示例：

```csv
姓名,参赛编号,组别,头像名称,领队姓名
张三,A001,初级组,avatar_001.jpg,李老师
李四,A002,高级组,avatar_002.jpg,王老师
```

## 📤 导出数据

导出的 `pfxt.zip` 文件包含：

- `data.csv` - 完整的选手数据（含检录状态、评分等）
- 评分照片文件（`作品码_分数.jpg`）

## 📝 License

MIT License
